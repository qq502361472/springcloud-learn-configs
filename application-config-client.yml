spring:
  profiles:
    active:
    - dev
---
server:
  port: 8201

spring:
  profiles: dev
  application:
    name: springcloud-config-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@localhost:7001/eureka
---
server:
  port: 8102

spring:
  profiles: beta
  application:
    name: springcloud-config-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@localhost:7001/eureka